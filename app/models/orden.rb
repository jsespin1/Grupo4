class Orden < ActiveRecord::Base

	def self.getOrdenes(oc)
		#ordenes = []
		#puts "Todo -> " + oc.inspect
		#oc.each do |e|
			#o = Orden.new(:_id => e['_id'...)
			#puts "ID -> " + e['_id']
		#end	

	 end
end
